# Сессия 2026-05-25 — Claude (claude.ai) — v3.2 Integration

## Что было сделано

### 1. Полный аудит репозитория
Прочитаны все файлы из leviathan_core_tar.gz, Download_tar.gz (agent_draft),
arbitr_cockpit_v0_5_tar.gz, leviathan_server_audit.md.

Изучена архитектура из аудита (SYSTEM ARCHITECTURE DOCUMENT v3.0):
ExecutionJournal, Idempotency, ResultEnvelope, PolicyEngine, CircuitBreaker.

### 2. Создан базовый v3.0 (первый пуш)
20 файлов, 2605 строк:
- config/settings.py          — Pydantic Settings, 14 Gemini ключей
- core_bridge/key_pool.py     — GeminiKeyPool + bridge к engine core
- execution/result_envelope.py — Structured Result Envelope (SAD §3)
- execution/idempotency.py    — OperationRegistry (sha256, TTL 7 дней)
- db/journal.py               — ExecutionJournal (паттерн arbitr_cockpit)
- db/storage.py               — TaskStorage с invocation_id
- agent/core.py               — LeviathanAgent (ReAct loop)
- main.py                     — FastAPI + WebSocket + lifespan

### 3. Аудит GitHub репо (второй пуш)
Обнаружены файлы от сессии 2026-05-24:
- agent/model_router.py       — ModelRouter (Gemini↔Claude AUTO/MANUAL)
- core_bridge/claude_adapter.py — Claude CLI + API + extended thinking
- agent/tools_arbitr.py       — 6 ArbitrCockpit инструментов
- mcp_server/leviathan_mcp.py — MCP server для Cursor
- docs/ полный set документации

### 4. Финальные фиксы (третий пуш, 5f8819b)
- agent/core.py: gemini-2.5-flash → gemini-2.0-flash
- mcp_server/leviathan_mcp.py: v1.1 (7 инструментов + HTTP transport)
  * leviathan_task, leviathan_status, leviathan_task_result, leviathan_logs
  * arbitr_lisa, arbitr_pipeline_status, arbitr_pipeline_advance
  * Двойной транспорт: stdio (Cursor) + --http PORT (Streamable HTTP)
- requirements.txt: добавлены rich, pyyaml
- .env.example: GEMINI_MODEL=gemini-2.0-flash

---

## Состояние репо на конец сессии (5f8819b)

### Все компоненты
| Файл | Статус | Описание |
|---|---|---|
| main.py | ✅ Production | FastAPI, lifespan, WS, ModelRouter+ClaudeAdapter инициализация |
| agent/core.py | ✅ Production | Gemini FC-loop + Claude routing + invocation_id + journal |
| agent/tools.py | ✅ | 8 базовых инструментов + claudethink |
| agent/tools_arbitr.py | ✅ | 6 ArbitrCockpit инструментов |
| agent/model_router.py | ✅ | AUTO/GEMINI_ONLY/CLAUDE_ONLY/GEMINI_THINK_CLAUDE/CLAUDE_THINK_GEMINI |
| agent/tg_bot.py | ✅ | Telegram: /task /safe /full /status /cancel |
| config/settings.py | ✅ | 14 Gemini + 5 Groq + Claude + MODE + ARBITR_URL |
| core_bridge/key_pool.py | ✅ | GeminiKeyPool async + bridge to engine core |
| core_bridge/claude_adapter.py | ✅ | CLI + API + extended thinking |
| db/journal.py | ✅ | ExecutionJournal (TaskRun→StepRecord→LLMCall) |
| db/storage.py | ✅ | TaskStorage |
| execution/idempotency.py | ✅ | OperationRegistry |
| execution/result_envelope.py | ✅ | SAD §3 envelope |
| mcp_server/leviathan_mcp.py | ✅ v1.1 | 7 tools, stdio+HTTP |
| leviathan_audit.py | ✅ | GitHub API audit/patch utility |

### Порты на сервере
```
8200 — Leviathan Agent (этот проект)
8090 — ArbitrCockpit
8005 — Orionyx
8000 — AI Outreach
8120 — VoiceStudio
8110 — KinoVibe
```

---

## Что НЕ сделано (для следующей сессии)

### ПРИОРИТЕТ 1: Деплой на сервер
```bash
# На root@78.17.24.96:
cd /opt/leviathan_engine
git clone https://github.com/lidenal85-blip/Leviathan_Agent.git agent_service
cd agent_service
python3 -m venv venv && source venv/bin/activate
pip install -r requirements.txt

# Тянем ключи из ядра
cat /opt/leviathan_engine/.env > .env
# Добавляем в .env:
echo "TG_BOT_TOKEN=8604646197:AAFJM_c38cGBxICgUDWB-jEtgImUz0Fyo2M" >> .env
echo "TG_ADMIN_CHAT_ID=7709651193" >> .env
echo "GITHUB_TOKEN=ghp_NEW_TOKEN" >> .env  # сменить!
echo "MODEL_MODE=AUTO" >> .env
echo "PORT=8200" >> .env
echo "ARBITR_URL=http://localhost:8090" >> .env

cp leviathan_agent.service /etc/systemd/system/
systemctl daemon-reload && systemctl enable leviathan_agent
systemctl start leviathan_agent
curl http://localhost:8200/health
```

### ПРИОРИТЕТ 2: React дашборд
Текущий дашборд — 300 строк inline HTML в main.py.
Вынести в /static/ или отдельный JSX-компонент.

### ПРИОРИТЕТ 3: GCE MCP
Пользователь хочет настроить Google Compute Engine MCP.
Инструкция в docs/SETUP_GCE_MCP.md.
После настройки — добавить tools_gce.py как отдельный toolset агента.

### ПРИОРИТЕТ 4: Обновить SYSTEM_PROMPT
Добавить в agent/core.py упоминание MCP HTTP транспорта (порт 8210).

